[Unit]
Description=TuVPN News Bot — предложки 3 раза в день (09/14/19 МСК)
Requires=tuvpn-news.service

[Timer]
# Сервер в UTC; слоты МСК 09,14,19 = UTC 06,11,16
OnCalendar=*-*-* 06,11,16:00:00
RandomizedDelaySec=600
Persistent=true

[Install]
WantedBy=timers.target
